import type { OceanEngine } from './OceanEngine';

/** Installs the optional subsystems (interaction tiles, bodies, shore, spray, scheduler). */
export function installStandardModules(engine: OceanEngine) {
  void engine;
}
